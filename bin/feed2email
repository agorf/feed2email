#!/usr/bin/env ruby

unless File.new('/tmp/feed2email.lock', 'w').flock(File::LOCK_NB | File::LOCK_EX)
  $stderr.puts 'An instance of feed2email is already running. Exiting...'
  exit 1
end

require 'feed2email/cli'

Feed2Email::Cli.start(ARGV)
